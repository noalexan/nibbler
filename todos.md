# TODOS

- Code snake logic
- Create 3 frontends and compile libs like `XX_libnibbler.so`
- Find 3 graphics libraries (SDL2, OpenGL, etc.)
- Subject impose `Makefile` but I would like `CMake` like compiler for library search logic
